const operations = require('./names.js')

const result = operations.add(1, 2, 4)
console.log(`the sum is ${result}`)

require('./fs_async.js')

//const result_minues = operations.sub(1000, 9998)
//console.log(`the subtrated result is ${result_minues}`)